{{#each projectData as |project| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{project.name}}</h2>
  </div>
  <div class="col-md-6">
    <p>{{project.description}}"</p>
  </div>
  <div>
    <h3>Funding Needed:{{project.needed_funding}}</h3>
  </div>
</div>
{{/each}}